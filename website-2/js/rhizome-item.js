class RhizomeItem {
  //   constructor(x1, y1, x2, y2, startObject, endObject) {
  constructor(x, y, title, pullquote, desc, links) {
    this.x = x;
    this.y = y;
    this.title = title;
    this.pullquote = pullquote;
    this.description = desc;
    this.links = links;
  }

  draw(drawNewRhizomeItem) {}
}
